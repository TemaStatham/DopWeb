{% for value in data  %}
    <p>{{ value }}</p>
{% endfor %}
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
<button type="submit" class="button">Отправить</button>
{{ form_end(form) }}